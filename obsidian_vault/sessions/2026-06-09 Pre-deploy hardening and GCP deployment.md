---
tags: [session, deployment, gcp, frontend, security]
date: 2026-06-09
---

# 2026-06-09 Pre-deploy hardening and GCP deployment

Pre-deploy fixes (cache TTL, Swagger docs, email copy), then full deployment to GCP VM. App running on `http://<EXTERNAL_IP>` via screen+uvicorn.

## What was done

### 1. Pre-deploy hardening (3 commits)
- **LinkedIn disk cache TTL**: set to 24h (`cache_ttl_seconds=86400`) — was infinite (None)
- **Swagger docs disabled**: `docs_url=None, redoc_url=None, openapi_url=None` on FastAPI — `/docs` and `/openapi.json` no longer exposed
- **End-dialog email copy fixed**: accepted popup says "will send shortly" (not "sent"), reject popup now mentions the follow-up email
- **pyproject.toml**: added `[tool.setuptools.packages.find] include=["backend*"]` — fixed flat-layout discovery error on `pip install .`

### 2. DEPLOY.md rewritten
- Python 3.12 (not 3.13, no deadsnakes PPA needed on Ubuntu 24.04)
- Real git URL (`Danil482/edra_lounge`)
- OS Login disable step (fix for "Permission denied (publickey)")
- Windows SSH fix (`putty_force_connect false`)
- scp with full paths (not `~` which creates literal tilde file)
- screen+uvicorn instead of systemd (simpler for demo)

### 3. GCP deployment completed
- SSH issue resolved: disabled OS Login via instance metadata, switched gcloud to OpenSSH
- Python 3.12 venv created, `pip install .` successful
- MiniLM model downloaded to `backend/models/`
- `.env` created on server via nano (scp unreliable with PuTTY/pscp on Windows)
- Database seeded (anonymized, no `SEED_WITH_NAMES`)
- uvicorn running via `sudo .venv/bin/python -m uvicorn backend.app:api --host 0.0.0.0 --port 80` in screen session
- `http://<EXTERNAL_IP>/health` verified working

### 4. Internal docs removed from repo
- `TASK.md` and `TASK_refactor_clustering.md` deleted — repo is now public-facing for reviewers

### 5. `data/viz_coords_2d.json` explained
- Pre-computed UMAP 2D coordinates for cluster scatter plot, generated by `seed_from_eval.py`
- New visitors interpolated as K=7 weighted average — avoids 45s UMAP recomputation per request
- Appeared in Phase 17 (2026-06-05)

## What was NOT done
- DNS subdomain (`demo.defygroup.ai`) — requested access from Ian
- HTTPS (Let's Encrypt) — deferred, HTTP sufficient for demo
- API key middleware — decided unnecessary for demo
- CORS lockdown to own domain — deferred until DNS is set up

## Key decisions
- **No API key for demo** — Swagger docs disabled is sufficient protection; no sensitive data
- **screen+uvicorn over systemd** — simpler, sufficient for demo lifecycle
- **Python 3.12** — Ubuntu 24.04 system Python, no PPA needed
- **Cache TTL 24h** — was infinite for dev testing, 86400s for production

## Open questions
- DNS access: waiting for Ian to share domain registrar login for `defygroup.ai`
- `demo.defygroup.ai` vs bare IP — depends on DNS access timeline
- HTTPS — needed before any real user traffic, but not for internal review
- VM auto-start after reboot — screen session doesn't survive reboot (upgrade to systemd if needed later)

## Next session — entry points
1. **DNS setup** — once Ian shares access, add A record for `demo.defygroup.ai`
2. **HTTPS** — Let's Encrypt via certbot or caddy reverse proxy
3. **Live quality check** — run 2-3 conversations on the deployed instance with gpt-4.1
4. **Full booth flow test** — auth gate → conversation → accept/decline → check Lemlist for lead
5. **Resume Lemlist campaign** in UI for the deployed instance

See [[../00-home/current priorities]]. Prior session: [[2026-06-08 GCP deploy IAM unblocked and firewall-VM created]].
