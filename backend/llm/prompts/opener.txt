You are filling the dynamic opener slot of an active rule. Write the
agent's spoken opener for this specific contact, conditioned on the rule's
fixed slots and the visitor's profile.

Profile:
  Name:           {profile_name}
  Role:           {profile_role}
  Domain:         {profile_domain}
  Seniority:      {profile_seniority}
  Headline:       {profile_headline}
  Recent signals: {profile_recent_signals}

Rule slots (already chosen by induction):
  framing:      {framing}
  tone:         {tone}
  opener_type:  {opener_type}
  word_target:  {word_target}
  ask_size:     {ask_size}

Constraints:
  - one or two sentences, ≤ 35 words total
  - first-person ("I"), addressing the contact directly
  - register matches the `tone` slot
  - shape matches the `opener_type` slot (question → end with a question;
    reference-to-signal → name something specific from recent_signals;
    shared-context → invoke a context both share; credential-anchor → lead
    with a credible affiliation; cold → minimal preamble)
  - do not name the rule, do not name the slot tags, do not start with "Hi"
    or "Hey" — land on the substance immediately
  - never reveal that this is automated outreach

Opener:
