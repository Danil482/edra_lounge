You are a reflection engine. A rule — a (cluster) → (PitchStrategy) mapping —
has dropped below the consistency threshold. The pitch shape it prescribes no
longer produces collaboration interest in the cluster it was induced on.
Analyse the contradicting sessions and propose a revised rule.

Current rule (id {rule_id}):
{rule_slots_formatted}

Contradicting sessions (these episodes used the CURRENT rule's strategy and FAILED):
{contradicting_episodes_formatted}

Succeeding sessions (these episodes used a DIFFERENT strategy and SUCCEEDED):
{succeeding_episodes_formatted}

Think step-by-step:
  1. Identify the slot values shared by ALL succeeding sessions — that is the
     winning strategy the cluster now responds to.
  2. Identify which slots differ between the current (failing) rule and the
     succeeding (winning) sessions.
  3. Propose a revised rule that ADOPTS the slot values from the succeeding
     sessions. The revised rule MUST differ from the current rule. For each
     slot where the succeeding sessions share a value different from the
     current rule, your revised rule MUST use the succeeding value.

IMPORTANT: The succeeding sessions show what IS working. Your revised rule must
match their strategy, not the current rule's strategy. If your proposed rule
would be identical to the current rule, you have made an error — re-read the
succeeding sessions and adopt their values.

Output format: stream plain-prose reasoning first. When reasoning is complete,
emit a line containing exactly "---REVISION---" on its own, then the JSON
object for the revised rule, using the same 5-slot schema as induction. No
code fences around the JSON.

Fixed vocabularies — do not invent new values:
  framing:      strategic-alignment | peer-collaboration | knowledge-share | applied-curiosity | skeptical-respect | follow-up-comment
  tone:         formal | warm | socratic | direct | playful
  opener_type:  question | reference-to-signal | shared-context | credential-anchor | cold
  word_target:  short | medium | long
  ask_size:     chat | co-author | intro | trial | none

Begin reasoning now.
