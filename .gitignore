__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/
build/
dist/

# local state
.env
edra_lounge.db
edra_lounge.db-journal
edra_lounge.db-wal
edra_lounge.db-shm
.cache/

# RapidAPI response cache (raw JSON per profile URL slug, see
# backend/profile_source/linkedin_rapidapi.py). Keep the directory itself
# via .gitkeep so the cache_dir exists in fresh checkouts.
data/linkedin_cache/*
!data/linkedin_cache/.gitkeep

# Dev-aid: raw RapidAPI responses for parser-debugging. Never committed.
data/linkedin_raw/

# editor
.vscode/
.idea/
.claude/

# runtime logs (uvicorn etc.)
*.log

# generated dataset dashboard (regenerable from research_profiles_master.csv)
research_profiles_dashboard.html
.dashboard_data.json
.compute_stats.py
.inject_data.py
